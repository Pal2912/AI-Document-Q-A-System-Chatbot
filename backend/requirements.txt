# ---------- Core Web Framework ----------
fastapi==0.138.0
uvicorn[standard]==0.49.0

# ---------- Settings & Validation ----------
pydantic==2.13.4
pydantic-settings==2.14.2
email-validator==2.3.0

# ---------- Database ----------
sqlalchemy==2.0.51
psycopg2-binary==2.9.12
alembic==1.18.4

# ---------- Authentication ----------
python-jose[cryptography]==3.5.0
bcrypt==5.0.0
python-multipart==0.0.32

# ---------- AI / RAG Stack ----------
# NOTE: LangChain 1.x restructured its module paths compared to 0.3.x
# (e.g. RecursiveCharacterTextSplitter now lives in langchain_text_splitters,
# not langchain.text_splitter). The versions below are the ones this
# project's code was actually written and tested against.
langchain==1.3.10
langchain-community==0.4.2
langchain-core==1.4.8
langchain-text-splitters==1.1.2
langchain-google-genai==4.2.5
langchain-huggingface==1.2.2
faiss-cpu==1.14.3
sentence-transformers==5.6.0

# ---------- PDF Processing ----------
pypdf==6.13.3

# ---------- Utilities ----------
# python-dotenv is a transitive dependency of pydantic-settings (used
# internally to parse .env files) — pinned explicitly here for clarity.
python-dotenv==1.2.2

# ---------- PyTorch (required by sentence-transformers) ----------
# This is a large dependency (~2GB with bundled CUDA libraries even on
# CPU-only systems, since no separate "cpu-only" wheel is published to
# the default PyPI index). Installing requirements.txt will need several
# GB of free disk space — see README for details.
torch==2.12.1
